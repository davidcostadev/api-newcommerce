import ProjectService from '../services/ProjectService';
import createResourceController from '../utils/createResourceController';

const controller = createResourceController(ProjectService);

export default controller;
